global_costmap:
#  global_frame: map
#  robot_base_frame: base_footprint

  static_map: true

#  update_frequency: 5.0
#  publish_frequency: 5.0
  update_frequency: 10.0
  publish_frequency: 10.0
  transform_tolerance: 0.5

# the value of update/publish frequency was set to 5 because the move_base node
# was sending warnings of "Map update loop missed its desidered rate of 10"